# ENTROPYHUB - TEST & DEMO KOMUTLARI
# ===================================

## 1) ORTAM
# ----------
# PowerShell
.\.venv\Scripts\Activate.ps1


## 2) TEMEL ÇALIŞTIRMA
# ---------------------
# Demo
python main.py

# API (varsa api modülü)
# uvicorn api.main:app --host 0.0.0.0 --port 8000 --reload


## 3) DOĞRULAMA
# --------------
# Bağımsız doğrulama
python tests\independent_validation.py

# Formal / bounded doğrulama
python formal\entropyhub_bounded_formal.py

# Gerçek ölçümlü benchmark
python benchmarks\real_entropyhub_benchmark.py

# Teknofest konsolide rapor
python docs\verification\generate_teknofest_report.py


## 4) PQC SANITY CHECK
# --------------------
python -c "from core.pqc.kyber768 import Kyber768; pk, sk = Kyber768.keygen(); ss1, ct = Kyber768.encaps(pk); ss2 = Kyber768.decaps(sk, ct); print('kyber-real', ss1==ss2)"

python -c "from core.pqc.dilithium3 import Dilithium3; pk, sk = Dilithium3.keygen(); sig = Dilithium3.sign(sk, b'hello'); print('dilithium-real', Dilithium3.verify(pk, b'hello', sig), len(sig))"


## 5) BLOCKCHAIN POC
# -------------------
# Oracle PoC
python blockchain\oracle_poc.py

# DLT PoC
python blockchain\dlt_layer.py

# Hızlı DLT doğrulama
python -c "from blockchain.dlt_layer import EntropyHubDLT; l=EntropyHubDLT(3); l.append_block({'type':'tx','amount':1}); l.append_block({'type':'tx','amount':2}); print('dlt-valid', l.verify_chain(), len(l.chain))"


## 6) RAPOR DOSYALARI
# --------------------
# - docs/verification/TEKNOFEST_ENTROPYHUB_REPORT.md
# - docs/verification/independent_validation_report.json
# - docs/verification/formal_bounded_report.json
# - benchmarks/benchmark_results_real.json
